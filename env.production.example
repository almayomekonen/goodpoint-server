# Authentication Configuration
ACCESS_TOKEN_NAME=access_token
TWO_FACTOR_TOKEN_COOKIE=two_factor_token
SECRET_OR_KEY=your_super_secret_key_change_this_in_production_123456789

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=z10mz10m
DB_NAME=good_point
DB_SSL=false

# Application Configuration
NODE_ENV=production
PORT=8080

# Database Options
DB_LOGGING=false
DB_SYNCHRONIZE=false

# Server Configuration
SERVER_DOMAIN=http://localhost:8080
CLIENT_DOMAIN=http://localhost:3000

# Email Configuration (optional for testing)
MAIL_USER=test@example.com
MAIL_PASSWORD=test_password
SEND_EMAIL=false

# SMS Configuration (optional for testing)
SEND_SMS=false

# Other Configuration
phone_shuffle=8,7,2,0,1,9,5,6,3,4
SEND_MONTHLY_EMAIL=false 